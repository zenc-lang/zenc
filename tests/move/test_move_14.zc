// EXPECT: PASS

struct M { val: int }

fn consume(m: M) {
  println "consume"
}

fn main(argc: int, argv: char**) {
  let m = M { val: 10 }

  if argc == 1 {
    consume(m)
  } else {
    consume(m)
  }
}
