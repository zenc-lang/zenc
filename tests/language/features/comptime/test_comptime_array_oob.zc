// EXPECT: FAIL
// language/features/comptime: an out-of-bounds comptime array index must be
// rejected at compile time
fn main() {
    comptime {
        let a: int[3];
        let i = 5;
        a[i] = 1;
    }
    return 0;
}
