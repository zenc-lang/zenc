
comptime {
    // Test __COMPTIME_TARGET__ 
    println "let TARGET = \"{__COMPTIME_TARGET__}\";";
    
    // Test __COMPTIME_FILE__
    println "let SOURCE = \"{__COMPTIME_FILE__}\";";
}

test "comptime_metadata_target" {
    assert(TARGET != NULL, "Target should not be NULL");
    println "Target platform: {TARGET}";
    println "Platform metadata works!";
}

test "comptime_metadata_source" {
    assert(SOURCE != NULL, "Source should not be NULL");
    println "Source file: {SOURCE}";
    println "Source file metadata works!";
}
