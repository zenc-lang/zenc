// EXPECT: FAIL

struct Mover {
  val: int
}

impl Drop for Mover {
  fn drop(self) {}
}

fn consume(m: Mover) {
  println "m.val = {m.val}"
}

fn main() {
  let m = Mover { val: 10 }
  consume(m)
  consume(m)
}
